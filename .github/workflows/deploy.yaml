name: "Actions Jhona"

on:
  push:
    branches: ["main"]

jobs:
  deploy_jhona:
    runs-on: ubuntu-latest
    steps:
      - name: Working dir
        run: ls -al

      - name: Action
        uses: actions/checkout@v4

      - name: Working after
        run: ls -al

  second_job:
    if: ${{ github.ref == 'refs/heads/main' }}
    needs: deploy_jhona
    runs-on: ubuntu-latest
    steps:
      - run: echo "second job"