name: "Actions Jhona"

on:
  push:
    branches: ["main"]

jobs:
  deploy-jhona:
    runs-on: ubuntu-latest
    step:
      - run: echo "general pull"
      - run: echo "install libraries"
      - run: echo "Run tests"