name: "Actions Jhona"

on:
  push:
    brances: ["main"]

jobs:
  deploy-jhona:
    runs-on: ubuntu-latest
    step:
      uses: actions/checkout@v4
      - run: echo "general pull"
      - run: echo "install libraries"
      - run: echo "Run tests"