name: "Actions Jhona"

on:
  push:
    branches: ["main"]

jobs:
  deploy_jhona:
    runs-on: ubuntu-latest
    steps:
      - run: echo "general pull"
      - run: echo "install libraries"
      - run: echo "Run tests"

  second_job:
    if: ${{ github.ref == 'refs/heads/main' }}
    needs: deploy_jhona
    runs-on: ubuntu-latest
    steps:
      - run: echo "second job"